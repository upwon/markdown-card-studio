export const sampleMarkdown = `# 把复杂知识，写成一组好看的卡片

Markdown Card Studio 是一个**本地优先**的卡片编辑器。左边调整样式，中间专心写作，右边实时预览。

## 为什么从 Markdown 开始？

Markdown 让内容与设计分离。你只需要维护一份纯文本，就可以反复切换主题、排版和尺寸。

> 好工具不会替你思考，但会让思考更容易被看见。

### 第一原则

- 内容永远是主角
- 主题只改变视觉，不改变文字
- 自动分页应该稳定、可预测
- 随时可以导回标准 Markdown

<!-- pagebreak -->

## 手动分页

在独立一行插入下面的注释，就能从新卡片开始：

\`\`\`md
<!-- pagebreak -->
\`\`\`

普通分隔线 \`---\` 仍然会渲染成水平线，不会意外分页。

### 常用语法

| 内容 | 写法 |
| --- | --- |
| 强调 | **重要文字** |
| 链接 | [Markdown Guide](https://www.markdownguide.org/) |
| 代码 | \`const idea = "ship it"\` |

最后，试着修改这段文字、切换主题，再导出你的第一组卡片吧。
`;
